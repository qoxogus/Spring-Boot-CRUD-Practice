spring:
  datasource:
    url: jdbc:mysql://the-database.caqd1opynsmq.ap-northeast-2.rds.amazonaws.com:3306/the_database
    username: the_user
    password: qoxogus1313
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  #org.hibernate.type: trace